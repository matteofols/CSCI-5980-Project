# To run this code
1. Open a terminal and run the all the server files using the following command
   `python server.py`
   `python server1.py`
   `python server2.py`   
2. Open another terminal and run the KVS.py file using the following command
   `python KVS.py`
